## build the docker image 
## push the docker imagekto the docker hub
## ssh into our vms machine and start a new image 


name: Deploy the backend 
on:
    push:
        branches: [ main ]
jobs:
    build:
        runs-on: ubuntu-latest